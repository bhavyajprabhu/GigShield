# Groq API key (free tier available at https://console.groq.com/keys)
# Powers the live AI advisor chat. Without this set, the app falls back
# to simpler rule-based chat responses automatically.
GROQ_API_KEY=your_groq_api_key_here
